import 'package:flutter/material.dart';

class BotPart extends StatelessWidget {
  const BotPart({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('this is bottom part');
  }
}
